use std::collection::VecDeque;




fn main(){
    let mut queue = VecDeque::new();
    let mut otherNodes = Vec::new();

    //node index, wallet from, wallet to, reward
    let tuple = (0, -1_i64, -1_i64);
    queue.push_back(tuple);

    

}


fn sendToOthers(){

}